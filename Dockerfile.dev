FROM node:22-alpine

WORKDIR /usr/src/app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy prisma schema for generation
COPY prisma ./prisma/
RUN npx prisma generate

# Copy source
COPY . .

EXPOSE 3000

CMD ["npm", "run", "start:dev"]